# pforge
pforge: A Packet Forge Description Language that allows a user to describe a data format and is converted to various forms that can traverse a network.

# Language semantics
This is a descriptive language not a programming language that you can create more AI slop. It is of a `.pf` extension.
A .pf file looks like this:
```
// A minimal ARP request packet
packet ArpRequest {
    u16be  hardware_type  = 0x0001;        // Ethernet
    u16be  protocol_type  = 0x0800;        // IPv4
    u8     hw_addr_len    = 6;
    u8     proto_addr_len = 4;
    u16be  operation      = 0x0001;        // Request
    u8[6]  sender_mac     = de:ad:be:ef:00:01;
    u32be  sender_ip      = 192.168.1.10;
    u8[6]  target_mac     = 00:00:00:00:00:00;
    u32be  target_ip      = 192.168.1.1;
}
```

As you can see there are no for loops or if statements, this made purely for describing protocols like in wireshark.
This is not an attempt at wireshark but a way building something for fun and profit of learning.
# The Output

Running ./pforge arp.pf on the above produces three sections:
1.Field dissection:

```
=== ArpRequest (28 bytes) ===
Offset  Bytes  Type    Field            Value
------  -----  ------  ---------------  --------------------
0x0000  2      u16be   hardware_type    0x0001
0x0002  2      u16be   protocol_type    0x0800
0x0004  1      u8      hw_addr_len      6
0x0005  1      u8      proto_addr_len   4
0x0006  2      u16be   operation        0x0001
0x0008  6      u8[6]   sender_mac       de:ad:be:ef:00:01
0x000e  4      u32be   sender_ip        192.168.1.10
0x0012  6      u8[6]   target_mac       00:00:00:00:00:00
0x0018  4      u32be   target_ip        192.168.1.1
```

2. Generated C struct:

```
/* Auto-generated by pforge */
#include <stdint.h>

typedef struct __attribute__((packed)) {
    uint16_t hardware_type;
    uint16_t protocol_type;
    uint8_t  hw_addr_len;
    uint8_t  proto_addr_len;
    uint16_t operation;
    uint8_t  sender_mac[6];
    uint32_t sender_ip;
    uint8_t  target_mac[6];
    uint32_t target_ip;
} ArpRequest;
```

3. Hex dump:
```
0000: 00 01 08 00 06 04 00 01 de ad be ef 00 01 c0 a8  ................
0010: 01 0a 00 00 00 00 00 00 c0 a8 01 01              ............
```

I have tried to to keep the grammar as small as possible inorder to also maintain readability.
This is a toy project nothing big intended from this code.
